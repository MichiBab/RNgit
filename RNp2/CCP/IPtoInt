struct in_addr addr;
    inet_pton(AF_INET, "127.0.0.1", &addr);
    printf("%d\n", addr.s_addr);
    
    int a = addr.s_addr;
    struct in_addr addr3;
    addr3.s_addr = a;
    char buf[16];
    inet_ntop(AF_INET, &addr3, buf, 16);
    printf("%s\n", buf);
